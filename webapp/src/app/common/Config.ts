const base =
  'http://www.zonipakhsh.ir/' ;
  //'http://localhost:5200/';
const baseApi = base + 'api/';
export const Urls = {
  Base: base,
  Token: base + 'token',
  Categories: baseApi + 'categories',
  Products: baseApi + 'products',
  Users: baseApi + 'users',
  Orders: baseApi + 'orders',
  Admins: baseApi + 'admins'
};
